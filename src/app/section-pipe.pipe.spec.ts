import { SectionPipePipe } from './section-pipe.pipe';

describe('SectionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SectionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
